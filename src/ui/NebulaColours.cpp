#include "NebulaColours.h"

// NebulaColours is header-only (inline constexpr definitions).
// This .cpp exists to ensure the translation unit is compiled
// and to provide a future home for derived palette utilities
// (e.g. theme-variant color generation, gradient builders).
