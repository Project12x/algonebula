// Bridge header: forwards ghostsun_render/SeparableLeniaEngine.h to the
// actual location in ghostsun examples. This bridge exists because the
// SeparableLeniaEngine.cpp uses this include path but the file lives in
// the ghostsun examples directory, not the public headers.
#include "../../../../ghostsun/render/examples/engines/SeparableLeniaEngine.h"
